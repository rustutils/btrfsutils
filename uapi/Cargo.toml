[package]
name = "btrfs-uapi"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { workspace = true }
bitflags = { workspace = true }
uuid = { workspace = true }

[build-dependencies]
bindgen = { workspace = true }
